//@flow
import type { Action } from "../actions/types";
import type { Event } from "../dataTypes";
import { check } from "../dataTypes";

export default (state: any = { hello: "world" }, action: Action): any => {
   return state;
};
